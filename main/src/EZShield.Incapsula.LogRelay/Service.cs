using System.ServiceProcess;

namespace EZShield.Incapsula.LogRelay
{
    /// <summary>
    /// Entry-point into running this tool as a detached Service
    /// (e.g. a Linux daemon or a Windows Service).
    /// </summary>
    public class Service
    {
        
    }
}